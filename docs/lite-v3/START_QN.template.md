# START QN

> 单一模式将 `N` 替换为题号；拆分模式将身份改为 `QN_K`（同题 START 后缀须从 1 连续且不得保留无后缀 START）。按建模依赖与执行边界选择粒度，不按题面小问数量机械拆分。删除所有提示。本文必须脱离原始对话独立可执行。

## 1. 问题目标与直接交付

<!-- 写明必须直接回答的问题和交付文件，不要只写方法名称。 -->

## 2. 已冻结输入与前问继承

**前问依赖：** 无

<!-- 使用“无”，或按题号、后缀严格排序列出更早官方问题的精确直接依赖，例如 Q1, Q2_1。无后缀 token 仅引用单一模式，带后缀 token 仅引用对应拆分单元；不允许通配符、自动展开或同题依赖。仅列出本单元实际输入和必要的上游结果路径，不复制上游建模口径。 -->

## 3. 数据口径与预处理

<!-- 定义观测单位、样本范围、缺失值、配对、归一化，以及会改变模型输入的数据异常和停止条件。 -->

## 4. 数学模型、参数与判定规则

<!-- 给出公式、参数、目标、约束、判定/破同分规则和允许的近似。 -->

## 5. Codex 执行边界

**不可变数学选择：**

<!-- 列出不得静默改变的数学含义、输入、参数、约束和决策规则。 -->

**Codex 可选实现细节：**

<!-- 列出不改变结论时可自行选择的语言、解析、排序、日志、绘图等实现细节。 -->

## 6. 输出与证据清单

<!-- 列出预期的 workspace-relative 文件；执行前这些文件无需存在。 -->

## 7. 验证、求解预算与停止规则

<!-- 写明运行时间、gap/迭代预算、允许的回退、验证项目和完成标准；未授权回退必须停止询问。 -->

## 8. 未决问题

无
